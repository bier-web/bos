/* BOS - BierOnStack - File Reserved */
headerProfileInfoTag = function (opts) {
	this.user = window.loggedUser;

	this.on('mount', function () {
		//riot.mount('.notifications', 'notification', {notifications: app.notificationCount});
	});
};
